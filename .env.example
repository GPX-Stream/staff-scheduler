# ============================================
# Authentication Configuration
# ============================================

# One-time setup key for seeding initial users
# Generate a random string: openssl rand -hex 32
# Used with POST /api/auth/setup endpoint
ADMIN_SETUP_KEY=your-random-setup-key-here

# Initial users configuration (JSON array)
# Set this in Vercel, then call POST /api/auth/setup once to seed users
# After seeding, this can be removed from Vercel env vars
# Example:
# USERS_CONFIG=[{"username":"jeff","displayName":"Jeff S.","password":"pass123","role":"admin"},{"username":"andy","displayName":"Andy I.","password":"pass123","role":"admin"},{"username":"alexx","displayName":"Alexx B.","password":"pass123","role":"admin"},{"username":"dane","displayName":"Dane B.","password":"pass123","role":"viewer"}]

# ============================================
# Upstash Redis (for server-side persistence)
# ============================================
# These are auto-configured by Vercel when you add Upstash Redis integration
KV_REST_API_URL=
KV_REST_API_TOKEN=

# ============================================
# DEPRECATED (no longer used)
# ============================================
# VITE_APP_PASSWORD - replaced by per-user authentication
